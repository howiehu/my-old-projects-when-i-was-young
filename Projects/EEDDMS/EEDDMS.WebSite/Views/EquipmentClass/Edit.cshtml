@model EEDDMS.Domain.Entities.EquipmentClass

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_EquipmentModuleLayout.cshtml";
}

<h2>编辑</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm("Edit", "EquipmentClass", FormMethod.Post))
{
    
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>设备分类</legend>

        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.ParentId)
        @Html.HiddenFor(model => model.SN)
        @Html.HiddenFor(model => model.IsDeleted)
        @Html.HiddenFor(model => model.CreationDate)
        @Html.HiddenFor(model => model.ModifyDate)
        @Html.HiddenFor(model => model.DeleteDate)

        <div class="editor-label">
            名称
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <p>
            <input type="submit" value="保存" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("返回设备分类列表", "Index")
</div>
